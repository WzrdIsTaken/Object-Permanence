using UnityEngine;
using UnityEngine.XR.Interaction.Toolkit;

namespace ObjectPermanence
{
    /**
     * Makes the controllers buzz a bit. Used, eg, when the player gets game ended.
     */
    public class HapticFeedbackComponent : MonoBehaviour
    {
        [SerializeField] 

        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}

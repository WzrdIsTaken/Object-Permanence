namespace ObjectPermanence
{
    /**
     * Used in DebugManager to set and filter assert and log message categories.
     */
    public enum DebugCategory
    {
        Audio,
        Debug,
        Level,
        VR
    }
}

namespace ObjectPermanence
{
    /**
     * A group of audio effects, for example levels might have 3 songs that can play.
     */
    public enum AudioGroupID : uint
    {
        Test,
        // Any other audio groups here!

        None
    }
}
